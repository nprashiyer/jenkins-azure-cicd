name: Demo

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Run a one-line script
        run: echo $env:GITHUB_REPOSITORY

      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo Repo name is $env:GITHUB_REPOSITORY
